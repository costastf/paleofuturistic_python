default_install_hook_types: [pre-commit, commit-msg]
repos:
  - repo: local
    hooks:
      - id: lint-commit
        name: Lint commit message
        description: "Example: feat: added new feature"
        entry: sh -c './workflow.cmd lint.commitizen --commit-msg-file="$1"' --
        language: system
        pass_filenames: true
        stages: [commit-msg]
      - id: format
        name: Format code and sort imports
        entry: sh ./workflow.cmd format
        language: system
        pass_filenames: false
        files: ^(_CI/tasks/|src/|tests/).*\.py$
        stages: [pre-commit]
      - id: ruff
        name: Lint with ruff
        entry: sh ./workflow.cmd lint.ruff
        language: system
        pass_filenames: false
        files: ^(_CI/tasks/|src/|tests/).*\.py$
        stages: [pre-commit]
      - id: pylint
        name: Lint with pylint
        entry: sh ./workflow.cmd lint.pylint
        language: system
        pass_filenames: false
        files: ^(_CI/tasks/|src/|tests/).*\.py$
        stages: [pre-commit]
      - id: ty
        name: Type check with ty
        entry: sh ./workflow.cmd lint.ty
        language: system
        pass_filenames: false
        files: ^(_CI/tasks/|src/|tests/).*\.py$
        stages: [pre-commit]
      - id: complexipy
        name: Complexity check with complexipy
        entry: sh ./workflow.cmd lint.complexipy
        language: system
        pass_filenames: false
        files: ^(src/).*\.py$
        stages: [pre-commit]
      - id: pyscn
        name: Quality check with pyscn
        entry: sh ./workflow.cmd quality.pyscn-check
        language: system
        pass_filenames: false
        files: ^(src/).*\.py$
        stages: [pre-commit]
      - id: test
        name: Run tests
        entry: sh ./workflow.cmd test
        language: system
        pass_filenames: false
        files: ^(src/|tests/).*\.py$
        stages: [pre-commit]
